@model TheGioiLoa.Models.Brand

@using (Ajax.BeginForm("EditBrand", "Admin", null, new AjaxOptions
{
    HttpMethod = "POST",
    OnBegin = "closeModal",
    OnSuccess = "editBrandSuccess",
    OnFailure = "editBrandFailure",
}, new { @id = "EditBrandForm" }))
{

    <div class="modal-header">
        <h4 class="modal-title" id="titleModal">Chỉnh sửa Thương Hiệu</h4>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">×</span>
        </button>
    </div>
    <div class="modal-body">
        @Html.HiddenFor(model => model.BrandId)
        <div class="form-group">
            <label for="Name">Tên danh mục</label>
            <input type="text" name="Name" id="Name" class="form-control input-form-name" required value="@Model.Name" />
        </div>
    </div>
    <div class="modal-footer justify-content-between">
        <button type="button" class="btn btn-default" data-dismiss="modal">Thoát</button>
        <button type="submit" class="btn btn-primary">Lưu Lại</button>
    </div>
}